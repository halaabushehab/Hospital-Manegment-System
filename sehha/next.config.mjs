// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;


/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.pinimg.com'], // إضافة النطاق الذي يستضيف الصور
    },
  };
  
  export default nextConfig;
  